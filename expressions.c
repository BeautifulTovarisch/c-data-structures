#include <stdio.h>
#include <string.h>

const char *infix_to_prefix(const char *expression) {
  int top = -1;

  const char op_stack = {0};
  const char *result = "";

  return "";
}

int main(int argc, const char *argv[argc]) {
  //

  return 0;
}
